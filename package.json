{
  "name": "openelis-review-tooling",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "grist": "node grist/grist-sync.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:widget": "playwright test",
    "check": "npm test && npm run test:widget"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0"
  }
}
