{
  "name": "openelis-review-tooling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bash scripts/check.sh",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
